# ------------------------------------------------------------
# Allow-list mode (keep only what you want in git)
# Tracks only: .cs, .pdf, .webp (+ this .gitignore file)
# ------------------------------------------------------------

# Ignore everything by default
*

# But don't ignore directories (so we can unignore files inside them)
!*/

# Keep these file types
!*.cs
!*.pdf
!*.webp

# Keep git ignore/config files
!.gitignore

# ------------------------------------------------------------
# Extra safety ignores (mostly redundant under allow-list mode)
# .NET / Visual Studio / Rider caches and build artifacts
# ------------------------------------------------------------

# Build outputs (all projects)
[Bb]in/
[Oo]bj/

# Visual Studio cache/options
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.VC.db
*.VC.VC.opendb

# Visual Studio Code (optional; comment out if you want to commit settings)
.vscode/

# MSBuild / publish artifacts
*.tlog
*.lastbuildstate
*.publishsettings
*.pubxml
*.pubxml.user

# NuGet (packages.config-era)
packages/
**/packages/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json

# Common build/test output folders
**/artifacts/
[Tt]est[Rr]esults/
*.trx
*.coverage
*.coveragexml

# Debug/Release folders (some project types output directly here)
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/

# Rider / IntelliJ
.idea/
*.sln.iml

# ReSharper
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# OS / tooling junk
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.tmp
*.temp
*.bak
*.swp
*.orig
*.log

# Debug symbols and intermediate artifacts
*.pdb
*.ipdb
*.iobj
*.cache
*.pidb
*.svclog
